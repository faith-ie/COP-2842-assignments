To compile the SCSS Style Sheet to CSS, you need to have Sass installed, which you can do with multiple ways.
https://sass-lang.com/install to find out more

or, alternatively you can just change the file extension from SCSS to CSS, because it's still CSS.